# aidlc-plugins-test

Throwaway marketplace used to test awslabs/aidlc-workflows PR #1104 (RFC #723).
Contents are the unmodified `dist/plugins/` tree emitted by `bun scripts/package.ts`:
`aidlc-marketplace.json` (schema v1), `.claude-plugin/` and `.codex-plugin/`
aggregates, and one projection per harness under `test-pro/`.
